# Web Channel

The Web UI is the primary demo and judging surface.

Pages:
- `/` landing page.
- `/runs/new` create a run.
- `/runs/[runId]` inspect the flight recorder timeline, risk ledger, approval gate, and report.
- `/dashboard` view recent runs and summary metrics.
